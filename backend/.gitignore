# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
server

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
build-errors.log

# Docker
.dockerignore

# Temporary files
tmp/
temp/

keys/
 
# Cursor IDE
# Ignore all Cursor metadata by default, but keep shared project rules tracked
.cursor/*
!.cursor/rules/
!.cursor/rules/**

# (Redundant with the wildcard above, kept for clarity if rules expand later)
.cursor/*.local.json
.cursor/settings.local.json
.cursor/history/
.cursor/sessions/
.cursor/cache/